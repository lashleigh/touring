<p id="notice"><%= notice %></p>
<%= @trip.title %></br>
<%= @trip.summary %></br>

<div class="rule"></div>
<% @trip.days.each do |day| %>
  <div class="day"><%= day.id%></div>
<% end %>
<div class="rule"></div>

<%= link_to 'Add Day', new_day_path %> |
<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
